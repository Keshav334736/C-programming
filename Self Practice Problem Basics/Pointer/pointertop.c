#include<stdio.h>
int main()
{
    float price=100.00;
    float *ptr=&price; //Pointer variable that holds the address of price
    float **pptr=&ptr; //Pointer to pointer variable that holds the address of ptr
    
}